services:
  server:
    build: ./src
    ports:
      - "4000:4000"
  client:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - ./src/.env
    depends_on:
      - server
  redis:
    image: "redis:alpine"

networks:
  mynetwork:
    driver: bridge
    external: true
